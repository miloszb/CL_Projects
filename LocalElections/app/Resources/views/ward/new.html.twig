{% extends 'base.html.twig' %}

{% block body %}
    <h2>Nowy obwód głosowania</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Zatwierdź" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('ward_index') }}">Wszystkie</a>
        </li>
    </ul>
{% endblock %}
