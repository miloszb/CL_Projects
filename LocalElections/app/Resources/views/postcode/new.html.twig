{% extends 'base.html.twig' %}

{% block body %}
    <h2>Dodaj kod pocztowy</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Zatwierdź" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('postcode_index') }}">Wszystkie</a>
        </li>
    </ul>
{% endblock %}
